# name: GitHub Actions 
# on:
#   schedule:
#     - cron: "* * * * *"
#   workflow_dispatch:
#   push:
#     branches:
#       - master
#   pull_request:
#     branches:
#         - master

# jobs: 
#     build:
#         runs-on: windows-latest
#         steps:
#             - name: Checkout Code
#               uses: actions/checkout@v6
#             - name: Say Hello
#               run: echo "Hello, World!"

name: Schedule Debug

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print time
        run: date



