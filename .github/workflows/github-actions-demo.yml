name: GitHub Actions 
on:
  schedule:
    - cron: "0 10 12 6 *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs: 
    build:
        runs-on: windows-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v6
            - name: Say Hello
              run: echo "Hello, World!"





